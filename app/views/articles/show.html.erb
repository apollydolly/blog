<% if @article %>
<h1><%= @article.title %></h1>

<p><%= @article.body %></p>
<!--
<ul>
  <li><%= link_to "Edit", edit_article_path(@article) %></li>
  <li><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>
-->

<ul>
  <li><%= link_to "Edit", edit_article_path(@article.title) %></li>
  <li><%= link_to "Destroy", article_path(@article.title), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
</ul>

<h2>Comments</h2>
<%= render @article.comments %>

<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to "Главная страница", root_path %>

<% else %>
  <p>Статья не найдена</p>
<% end %>
